sudo: enabled

language: python

python:
  - 3.6

notifications:
  email:
    recipients:
      
  on_success: never
  on_failure: always

install:
  - pip install pipenv
  - pipenv install --dev
before_install: 

script:
  - export APP_SETTINGS=app.config.TestingConfig
  - pipenv run python -W ignore::DeprecationWarning -m unittest -v api_test/login_api_test.py
  - pipenv run python -W ignore::DeprecationWarning -m unittest -v api_test/user_api_test.py

before_deploy: 

deploy:
