sudo: enabled

language: python

python:
  - 3.6

notifications:
  email:
    recipients:
      
  on_success: never
  on_failure: always

install:
  - pip install pipenv
  - pipenv install --dev
before_install: 

script:
  - export APP_SETTINGS=app.config.TestingConfig
  #- pipenv run python -W ignore::DeprecationWarning -m unittest -v tests/login_api_test.py
  #- pipenv run python -W ignore::DeprecationWarning -m unittest -v tests/user_api_test.py
  - pipenv run nosetests tests\ --with-coverage --cover-package=app
after_success:
  - coveralls

before_deploy: 

deploy:
